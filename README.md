Once you've install Python, make sure to use the console in the folder this is downloaded to.

Then, copy and paste:
```
pip install -r "requirements.txt"
```
into your console to download the required modules.
